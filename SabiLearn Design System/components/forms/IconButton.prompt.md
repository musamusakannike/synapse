Circular icon-only button for toolbars, headers, and dismiss actions. Renders a Lucide glyph.

```jsx
<IconButton icon="menu" aria-label="Open menu" onClick={() => {}} />
```

`variant="filled"` for a primary-green circular button (e.g. play/record); default `ghost` for chrome.
