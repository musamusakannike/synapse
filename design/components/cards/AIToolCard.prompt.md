Entry point card for one AI feature (Summarizer, Quiz generator, Flashcards generator, Q&A AI). Violet fill is reserved for AI surfaces — never used for plain course content. Icon should be a Heroicons-style outline glyph, never a sparkle or emoji.

```jsx
<AIToolCard icon={<DocumentIcon />} title="Summarizer" description="Turn any lecture note into a short summary." cta="Summarize a note" />
```
